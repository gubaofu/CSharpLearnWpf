<Window x:Class="WpfAppWvvmPrismDemo.Views.RestaurantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppWvvmPrismDemo.Views"
        xmlns:prism="http://prismlibrary.com/"
        mc:Ignorable="d"
        Title="{Binding Restaurant.Name, StringFormat=\{0\}-在线订餐}" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        prism:ViewModelLocator.AutoWireViewModel="True">
    <Border BorderBrush="Orange" BorderThickness="6" CornerRadius="6" Background="Yellow">
        <Grid x:Name="Root" Margin="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Border BorderBrush="Orange" BorderThickness="2" CornerRadius="6" Padding="4">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <StackPanel.Effect>
                            <DropShadowEffect Color="LightGray"/>
                        </StackPanel.Effect>
                        <TextBlock FontSize="60" FontFamily="Lishu" Text="欢迎光临-"/>
                        <TextBlock FontSize="60" FontFamily="Lishu" Text="{Binding Restaurant.Name}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="24" FontFamily="Lishu" Text="小店地址："/>
                        <TextBlock FontSize="24" FontFamily="Lishu" Text="{Binding Restaurant.Address}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="24" FontFamily="Lishu" Text="订餐电话："/>
                        <TextBlock FontSize="24" FontFamily="Lishu" Text="{Binding Restaurant.PhoneNumber}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            <DataGrid Grid.Row="1" Margin="4" FontSize="16" ItemsSource="{Binding DishMenu}"
                  AutoGenerateColumns="False" CanUserDeleteRows="False" GridLinesVisibility="None">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="菜品" Width="120" Binding="{Binding Dish.Name}"/>
                    <DataGridTextColumn Header="种类" Width="120" Binding="{Binding Dish.Category}"/>
                    <DataGridTextColumn Header="点评" Width="120" Binding="{Binding Dish.Comment}"/>
                    <DataGridTextColumn Header="评分" Width="120" Binding="{Binding Dish.Score}"/>
                    <DataGridTemplateColumn Header="选中" Width="120" SortMemberPath="IsSelected">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Path=IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                      VerticalAlignment="Center" HorizontalAlignment="Center"
                                      Command="{Binding Path=DataContext.SelectMenuItemCommand, 
                                RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type DataGrid}}}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="共计" VerticalAlignment="Center"/>
                <TextBox Width="120" Margin="4" IsReadOnly="True" TextAlignment="Center" Text="{Binding Count}"/>
                <Button Content="Order" Height="24" Width="120" Command="{Binding OrderCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
